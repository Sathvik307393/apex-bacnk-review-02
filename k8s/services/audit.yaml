# ============================================================
# Audit Service — Deployment + ClusterIP Service (INTERNAL)
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexa-audit
  namespace: nexabank
  labels:
    app: nexa-audit
    component: audit
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nexa-audit
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: nexa-audit
        component: audit
    spec:
      containers:
        - name: audit
          image: apexbankregistry.azurecr.io/nexa-audit:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3006
          envFrom:
            - configMapRef:
                name: nexa-config
          env:
            - name: PORT
              value: "3006"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: nexa-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: nexa-secrets
                  key: JWT_SECRET
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets-store"
              readOnly: true
          livenessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "azure-kvname-user-msi"
---
apiVersion: v1
kind: Service
metadata:
  name: nexa-audit
  namespace: nexabank
  labels:
    app: nexa-audit
spec:
  type: ClusterIP
  selector:
    app: nexa-audit
  ports:
    - name: http
      port: 3006
      targetPort: 3006
      protocol: TCP



