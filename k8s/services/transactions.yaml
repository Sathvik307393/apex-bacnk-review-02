# ============================================================
# Transactions Service — Deployment + ClusterIP Service (INTERNAL)
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexa-transactions
  namespace: nexabank
  labels:
    app: nexa-transactions
    component: transactions
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nexa-transactions
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: nexa-transactions
        component: transactions
    spec:
      containers:
        - name: transactions
          image: apexbankregistry.azurecr.io/nexa-transactions:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3003
          envFrom:
            - configMapRef:
                name: nexa-config
          env:
            - name: PORT
              value: "3003"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: nexa-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: nexa-secrets
                  key: JWT_SECRET
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets-store"
              readOnly: true
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "azure-kvname-user-msi"
---
apiVersion: v1
kind: Service
metadata:
  name: nexa-transactions
  namespace: nexabank
  labels:
    app: nexa-transactions
spec:
  type: ClusterIP
  selector:
    app: nexa-transactions
  ports:
    - name: http
      port: 3003
      targetPort: 3003
      protocol: TCP



